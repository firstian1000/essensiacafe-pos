{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "scripts": {
        "build": "vite build && node -e \"require('fs').mkdirSync('dist',{recursive:true})\"",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^3.1",
        "tailwindcss": "^4.0.0",
        "vercel-php": "^0.9.0",
        "vite": "^8.0.0"
    }
}
